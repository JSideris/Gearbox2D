\documentclass{jcgt}

\setciteauthor{Joshua Sideris}
\setcitetitle{Kinematic Restitution Balancing: A Low-Cost Analytical Fix for Energy Gain in Velocity-Level Impulse Solvers}

\submitted{\today}

\begin{document}

\title{Kinematic Restitution Balancing (KRB)\\\large A Low-Cost Analytical Fix for Energy Gain in Velocity-Level Impulse Solvers}

\author{Joshua Sideris\\Gearbox2D\\\href{https://gearbox2d.com}{https://gearbox2d.com}}

\maketitle
\thispagestyle{firstpagestyle}

\begin{abstract}
\small
In discrete physics simulations using velocity-level impulse solvers (Sequential Impulse / Projected Gauss-Seidel), energy gain is a common numerical artifact. This paper introduces \textbf{Kinematic Restitution Balancing (KRB)}, a per-constraint correction for that leak in both unilateral constraints (collisions) and bilateral constraints (joints). By compensating for force-induced velocity drift (Component~A) and auditing potential-energy shifts during position correction (Component~B), KRB removes the two usual analytic energy sources inside a single-pass SI/PGS pipeline, without a second position pass. It does not claim a global energy invariant; evaluation and limitations sections state the coupled-constraint gap that follows from the per-constraint audit.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Velocity-level sequential-impulse (SI) solvers \cite{Catto2005} are the standard real-time rigid-body approach: constraints are solved after explicit force integration with a fixed iteration count. KRB is a drop-in \emph{preSolve} energy audit for that pipeline. It does not add solver passes, replace PGS, or switch to position-based dynamics.

Two analytic sources inject artificial mechanical energy during constraint resolution.

\paragraph{Force-integration drift.}
With symplectic Euler integration, velocity is updated before the constraint solve:
\begin{equation}
v_{\mathrm{solver}} = v_t + a_{\mathrm{ext}} \cdot \Delta t.
\label{eq:force_drift}
\end{equation}
Restitution and joint bias see $v_{\mathrm{solver}}$ instead of the true relative velocity $v_{\mathrm{impact}}$. For a perfect bounce ($e=1$), the launch velocity gains $a_{\mathrm{ext}} \cdot \Delta t$ extra speed per frame. For a joint at rest, the solver sees gravity-induced velocity and fights it every frame at the velocity level.

\paragraph{Potential-energy shift from position correction.}
Baumgarte stabilization \cite{Baumgarte1972} feeds constraint error back as a velocity bias $v_{\mathrm{bias}} = \beta C / \Delta t$, displacing bodies by $\Delta h$ to resolve penetration or joint drift. That displacement increases potential energy ($mgh$) without reducing kinetic energy. The added PE converts to KE on later frames, causing runaway energy growth.

Split impulse and equivalent decoupled position passes \cite{Catto2014} keep position-correcting impulses off the physical velocity used for restitution, which removes the \emph{immediate} bounce-from-correction artifact. They do not tax the potential-energy work of the remaining displacement $\Delta h$. Nonlinear Gauss--Seidel (NGS) position solvers likewise omit that audit \cite{Catto2024}. Component~B closes that gap inside a single-pass SI profile. KRB is not a dual-pass temporal solver, variational integrator, or PBD/XPBD stack.

KRB applies an analytic PE/KE balance to the \emph{expected} correction displacement $\Delta h$ at constraint setup, for contacts and distance joints. The audit is per-constraint; coupled graphs can still exhibit offsets when one constraint forces work on another (see evaluation and limitations).

\section{Method}
\label{sec:method}

KRB performs two independent corrections during constraint \texttt{preSolve}.

\subsection{Component A: Force velocity compensation}
\label{sec:component_a}

Store the velocity increment from external forces during integration ($v_{\mathrm{force}} = a_{\mathrm{ext}} \cdot \Delta t$ per body) and compute the true relative normal velocity:
\begin{equation}
v_{\mathrm{impact}} = v_{\mathrm{relative}} - (v_{\mathrm{force},B} - v_{\mathrm{force},A}).
\label{eq:component_a}
\end{equation}
Component~A applies to all constraints whenever symplectic Euler integration is used, independent of the bias method.

\subsection{Component B: Kinematic energy balancing}
\label{sec:component_b}

Adjust the launch or bias velocity to account for work done by external forces over the expected correction displacement $\Delta h$:
\begin{equation}
v_{\mathrm{surf}} = \sqrt{\max\bigl(0,\, v_{\mathrm{impact}}^2 + 2 (\mathbf{a}_{\mathrm{ext}} \cdot \mathbf{n}) \Delta h\bigr)},
\label{eq:vsurf}
\end{equation}
\begin{equation}
v_{\mathrm{final}} = e \cdot v_{\mathrm{surf}} = e \sqrt{\max\bigl(0,\, v_{\mathrm{impact}}^2 + 2 (\mathbf{a}_{\mathrm{ext}} \cdot \mathbf{n}) \Delta h\bigr)}.
\label{eq:vfinal}
\end{equation}
For ground collisions ($\mathbf{a}_{\mathrm{ext}} \cdot \mathbf{n} < 0$), Component~B taxes launch velocity to pay for increased PE; for ceiling collisions ($\mathbf{a}_{\mathrm{ext}} \cdot \mathbf{n} > 0$), it credits velocity for work against external forces.

\subsection{Effective displacement prediction}
\label{sec:deff}

When velocity and position phases are decoupled, the kinematic bounce velocity $v_{\mathrm{launch}}$ partially resolves penetration $d$ before the position solver runs:
\begin{equation}
d_{\mathrm{eff}} = \max\bigl(0,\, d - (v_{\mathrm{launch}} \cdot \Delta t)\bigr).
\label{eq:deff}
\end{equation}
Many engines clamp per-iteration position correction to $\Delta h_{\max}$ (e.g.\ \texttt{MAX\_POSITION\_CORRECTION}). The audit must match the work actually performed:
\begin{equation}
\Delta h = \min(d_{\mathrm{eff}}, \Delta h_{\max}) \cdot \Gamma,
\label{eq:deltah}
\end{equation}
where $\Gamma = 1 - (1 - \beta)^n$ for $n$ position iterations with Baumgarte factor $\beta$.

\section{Constraint application}
\label{sec:constraints}

\subsection{Contacts and speculative gaps}
\label{sec:contacts}

For physical overlap ($d > 0$), both Component~A and Component~B are required.

Speculative contacts are created before overlap ($d < 0$, a gap) to prevent tunneling. Because no position-correction work occurs yet, $\Delta h = 0$ and Component~B is omitted; Component~A remains critical so gravity-induced velocity is not treated as impact speed.

Apply restitution bias only when restitution is enabled and either the corrected normal velocity exceeds a small threshold, or a speculative gap is closing faster than the gap width allows. Resting contacts must not receive an $e=1$ launch bias.

\subsection{Distance joints and the zero-velocity paradox}
\label{sec:joints}

Distance joints target zero relative velocity along the rod. Omitting Component~B entirely leaks energy when Baumgarte stretch correction does work against gravity. Component~B must be applied, but capped so a resting joint is not paralyzed.

Let $v_{\mathrm{bias}} = \beta C / \Delta t$ be the ideal correction velocity for constraint error $C$. The audited correction is
\begin{equation}
v_{\mathrm{bias\_actual}} = \sqrt{\max\bigl(0,\, v_{\mathrm{bias}}^2 - 2 (\mathbf{a}_{\mathrm{ext}} \cdot \mathbf{n}) (\beta C)\bigr)}.
\label{eq:joint_bias}
\end{equation}
If kinetic energy cannot pay the PE tax, the joint retains a microscopic Baumgarte sag---bounded audit rather than infinite stiffness at rest. This paper's setup recipe covers contacts and \emph{distance} joints; other joint types are outside the listed implementation.

\section{Implementation}
\label{sec:implementation}

KRB runs at contact and distance-joint \texttt{preSolve}, before the velocity iteration loop. Prerequisites: symplectic Euler with per-body \texttt{forceVelocity} ($a_{\mathrm{ext}} \Delta t$), Baumgarte position correction with factor $\beta$, position iteration count $n$, and penetration slop.

\subsection{Contact setup}
\label{sec:impl_contacts}

\begin{lstlisting}[caption={Contact \texttt{preSolve} bias (Component A + B).},label={lst:contact}]
// Component A: true impact normal velocity
forceVn = dot(bodyB.forceVelocity - bodyA.forceVelocity, n)
relativeVn = vn - forceVn

vBounce = -e * relativeVn
shouldBounce = enableRestitution &&
  (relativeVn < -RESTITUTION_THRESHOLD ||
   (depth < 0 && relativeVn < depth / dt))

if (shouldBounce) {
  if (depth > 0) {
    depthAfterVelocity = max(0, (depth - slop) - vBounce * dt)
    cumulativeFactor = 1 - pow(1 - beta, n)
    expectedDisplacement =
      min(depthAfterVelocity, MAX_POSITION_CORRECTION) * cumulativeFactor
  } else {
    expectedDisplacement = 0  // speculative: Component A only
  }
  accVn = forceVn / dt
  workTerm = 2 * accVn * expectedDisplacement
  vSurfSq = relativeVn * relativeVn + workTerm
  vFinal = e * sqrt(max(0, vSurfSq))
  bias = (depth < 0) ? -max(vFinal, depth / dt) : -vFinal
} else if (depth < 0) {
  bias = -depth / dt
} else {
  bias = 0
}
\end{lstlisting}

\subsection{Distance-joint setup}
\label{sec:impl_joints}

\begin{lstlisting}[caption={Distance-joint \texttt{preSolve} bias.},label={lst:joint},language=C++]
float forceVn = (bodyB->forceVelocity - bodyA->forceVelocity).dot(normal);
float v_bias_ideal = beta * C / dt;
float expectedDisplacement = v_bias_ideal * dt;
float accVn = forceVn / dt;
float workTerm = 2.0f * accVn * expectedDisplacement;
float v_bias_sq = v_bias_ideal * v_bias_ideal;
float adjusted_v_bias_sq = max(0.0f, v_bias_sq - workTerm);
float v_bias_actual = sqrt(adjusted_v_bias_sq);
bias = (v_bias_ideal > 0 ? v_bias_actual : -v_bias_actual) - forceVn;
\end{lstlisting}

During the velocity solve, use the stored bias; Component~A's $-\mathrm{forceVn}$ is folded into \texttt{bias} so \texttt{relative\_vn} is not double-corrected each iteration.

\section{Evaluation}
\label{sec:evaluation}

Figure~\ref{fig:ablation} is a compile-time ablation of the Gearbox2D sequential-impulse solver: default KRB on versus the same sources built with \texttt{-DGEARBOX\_DISABLE\_KRB}. Figure~\ref{fig:engines} places that KRB-on build next to unmodified Box2D-WASM, p2.js, and Matter.js. Dataset~C, an $8\,\mathrm{h}$ KRB-on continuation of the cradle and a contact-only enclosure check, has no figure; traces are in \texttt{data/dataset-c/}. We do not report a patched-Box2D result.

Every scene runs for $600\,\mathrm{s}$ of simulation time at $dt = 1/60$, $e = 1$, zero friction and damping, sleep disabled. Mechanical energy is $E = E_k + E_p$. Dataset~A includes rotational KE; Dataset~B is translational only (the website adapters do not expose $\omega$). On the overlapping Gearbox traces the two agree to about $10^{-6}$ for the floor bounce. The reported ratio is $E/E_0$ relative to the known start-of-run energy.

\begin{figure}[htb]
  \centering
  \includegraphics[width=\columnwidth]{../figures/fig-energy-ablation.pdf}
  \caption{Mechanical energy ratio $E/E_0$ over $600\,\mathrm{s}$ of simulation time. (a)~Single elastic floor contact. (b)~High-rate enclosure; KRB on is a $60\,\mathrm{s}$ windowed mean, KRB off is raw $1\,\mathrm{Hz}$ samples terminating at tunneling ($t = 2.7\,\mathrm{s}$). (c)~Newton's cradle (coupled contacts and joints). Vector original: \texttt{figures/fig-energy-ablation.pdf}.}
  \label{fig:ablation}
\end{figure}

\small
\begin{table}[htb]
  \caption{Dataset~A ablation: $E/E_0$ at $600\,\mathrm{s}$ (compile-time KRB on/off).}
  \label{tab:dataset-a}
  \centering
  \begin{tabular}{@{}lclrrl@{}}
    \hline
    Scene & KRB & $E/E_0$ & Win.\ mean & Outcome \\
    \hline
    Floor bounce & on & $0.999$ & $1.000$ & bounded \\
    Floor bounce & off & $6.53$ & climbing & runaway \\
    High-pressure circle & on & --- & $1.00$ & stayed in box \\
    High-pressure circle & off & --- & --- & escaped, step $161$ \\
    Newton's cradle & on & $1.07$ & $1.05$ after $t=300$ & bounded offset \\
    Newton's cradle & off & $1.79$ & climbing & secular gain \\
    \hline
  \end{tabular}
\end{table}

Contact-only scenes match Section~\ref{sec:introduction}. With KRB, the floor bounce stays at $E/E_0 = 1.000 \pm 0.001$ in every $60\,\mathrm{s}$ window and never exceeds its start height. Without KRB the same drop gains about $0.55\,E_0$ per minute and finishes at $E/E_0 = 6.53$, with the apex 50 length units above the release. The high-pressure enclosure is the same leak at a higher impact rate: KRB window means stay at $1.00$ for the full run; without KRB the body leaves the box after $2.7\,\mathrm{s}$ at $E/E_0 \approx 10$. Instantaneous $1\,\mathrm{Hz}$ samples of the on-curve swing between $0.67$ and $1.33$ because they alias the bounce; the windowed mean is the drift signal.

The cradle is better with KRB but is not an invariant, which is the coupled-constraint gap in Section~\ref{sec:limitations}. Without KRB, $E/E_0$ climbs from $1.00$ to $1.79$ and outer-ball peak height from $1.35$ to $2.43$. With KRB, energy sits slightly low ($\sim 0.96$) for four minutes, steps to $\sim 1.07$ around $t = 240$--$300\,\mathrm{s}$, then plateaus or slightly decays ($1.072 \to 1.054$). That is a bounded offset, not the linear SI ramp. A headless $8\,\mathrm{h}$ continuation (Dataset~C) stays in that band: after the step, $E/E_0 \in [1.034, 1.093]$ with mean $1.053$; the last hour matches the first hour after the step, and there are no further steps. The contact-only enclosure check in Dataset~C stayed inside the box for $8\,\mathrm{h}$; $600\,\mathrm{s}$ windowed $E/E_0$ drifted $1.009 \to 0.977$ (slow loss, not the SI gain of Figure~\ref{fig:ablation}b).

Resting joint chains remain stationary with Component~A enabled (no spurious force-drift correction at the velocity level). High-frequency spring modes are likewise not numerically damped by force-integration drift. Those two observations are qualitative and are not part of Figure~\ref{fig:ablation}.

\begin{figure}[htb]
  \centering
  \includegraphics[width=\columnwidth]{../figures/fig-energy-engines.pdf}
  \caption{Mechanical energy ratio $E/E_0$ over $600\,\mathrm{s}$ on unmodified engines. (a)~Elastic floor contact. (b)~Newton's cradle. Gearbox2D is the default KRB-on WASM build; Box2D-WASM, p2.js, and Matter.js are unmodified whole-engine traces, not an in-engine KRB ablation. Vector original: \texttt{figures/fig-energy-engines.pdf}.}
  \label{fig:engines}
\end{figure}

Dataset~B is whole-engine behavior: iteration counts, split impulse, restitution thresholds, and default damping all differ. It is not an ablation of KRB inside those engines. Traces are in \texttt{data/dataset-b-*-600s.csv}.

\small
\begin{table}[htb]
  \caption{Dataset~B: $E/E_0$ at $600\,\mathrm{s}$ on unmodified engines.}
  \label{tab:dataset-b}
  \centering
  \begin{tabular}{@{}llrl@{}}
    \hline
    Scene & Engine & $E/E_0$ & Outcome \\
    \hline
    Floor bounce & Gearbox2D (KRB on) & $0.999$ & bounded \\
    Floor bounce & Box2D-WASM & $6.61$ & runaway \\
    Floor bounce & p2.js & $0.376$ & slow loss \\
    Floor bounce & Matter.js & $\approx 0$ & dead by $t\approx 160\,\mathrm{s}$ \\
    Newton's cradle & Gearbox2D (KRB on) & $1.062$ & bounded offset \\
    Newton's cradle & Box2D-WASM & $0.221$ & stepped loss \\
    Newton's cradle & p2.js & $0.320$ & slow loss \\
    Newton's cradle & Matter.js & $\approx 0$ & dead by $t\approx 40\,\mathrm{s}$ \\
    \hline
  \end{tabular}
\end{table}

The floor bounce is the contact leak from Section~\ref{sec:introduction} in a production SI engine. Box2D-WASM climbs to $E/E_0 = 6.61$, matching Gearbox with KRB off ($6.53$) to a few percent. Gearbox with KRB on stays at $1.000 \pm 0.001$ in every $60\,\mathrm{s}$ window, as in Figure~\ref{fig:ablation}a. p2.js and Matter.js fail the other way: they dissipate, Matter reaching rest by $t\approx 160\,\mathrm{s}$.

The cradle is a different regime---elastic contacts plus joints---and the other engines lose energy rather than gain it. Matter is done by $t\approx 40\,\mathrm{s}$. Box2D-WASM falls in steps to $0.22$ (restitution velocity threshold plus Baumgarte on the rods). p2.js drains smoothly to $0.32$. Gearbox repeats Figure~\ref{fig:ablation}c: a $\sim 7\%$ step near four minutes, then a plateau. That is the empirical bound in Section~\ref{sec:limitations}, not a claim that the other engines share the SI gain of Figure~\ref{fig:engines}a.

Energy loss on the cradle is not only a smaller amplitude. Unmodified Box2D, p2, and Matter lose the single-ball transfer: two balls leave together, or the pack splits, and the outer-ball stroke drops. Gearbox keeps the one-at-a-time exchange for the full $600\,\mathrm{s}$. That is a qualitative observation; Figure~\ref{fig:engines}b reports $E/E_0$.

A high-pressure enclosure ($g = 400$) was logged with the same harness and omitted from Figure~\ref{fig:engines}. Matter rests on the floor within a second; p2.js does so within about $90\,\mathrm{s}$; Box2D-WASM locks to a two-value limit cycle ($1.000$ / $0.828$ on alternate seconds) consistent with split impulse and a translation cap. That is a solver-survival test, not an energy comparison.

\section{Limitations}
\label{sec:limitations}

KRB is a per-constraint audit, not a coupled one. When resolving one constraint (for example a horizontal collision) forces a second constraint to do work against gravity (for example a pendulum rod lifting the body), the isolated PE/KE tax may miss the systemic change in potential energy.

Newton's cradle is that coupled case. In Figure~\ref{fig:ablation}c and Figure~\ref{fig:engines}b the KRB run shows a $\sim 7\%$ energy step near four minutes, then a plateau for the rest of the $600\,\mathrm{s}$ window---not a secular climb, and not a proof of a global invariant. Dataset~C continues that KRB-on cradle to $8\,\mathrm{h}$ of simulation time ($dt = 1/60$, sampled every $10\,\mathrm{s}$; traces in \texttt{data/dataset-c/}). After the same four-minute step, $E/E_0$ stays in $[1.034, 1.093]$ (mean $1.053$) through $t = 28800\,\mathrm{s}$; the last-hour mean is $1.053$, identical to the hour after the step, and $60\,\mathrm{s}$ windowed means do not jump again. That is an empirical multi-hour bound for this scene, still not a global invariant, and still not shown for arbitrary contact/joint graphs.

The same $600\,\mathrm{s}$ of unmodified Box2D, p2, and Matter (Figure~\ref{fig:engines}b) dissipate rather than gain; that is a different failure mode and does not close the coupled-audit gap. Variational integrators remain a different claim.

A coupled-constraint audit is future work. High-speed tunneling, long ill-conditioned chains, and other scenes that fail for reasons other than the two leaks in Section~\ref{sec:introduction} are outside the claim. The no-KRB enclosure escape in Figure~\ref{fig:ablation}b is the SI leak at high impact rate, not a KRB failure.

\section{Conclusion}

Kinematic Restitution Balancing is a drop-in correction for velocity-level sequential-impulse solvers \cite{Catto2005}. Component~A removes force-integration drift from restitution and joint bias; Component~B taxes (or credits) the launch or bias velocity for the potential-energy work of the expected correction displacement $\Delta h$. Both apply to contacts and joints, at the cost of a few extra scalars per constraint setup.

That is a narrower claim than a dual-pass solver or a reduced-coordinate formulation. Split impulse and NGS \cite{Catto2014,Catto2024} still hide Baumgarte bounce by keeping position work off the physical velocity; KRB does not replace that machinery, and it does not match their convergence behavior. It cancels the two analytic leaks in Section~\ref{sec:introduction} inside a single-pass SI profile, with the coupled-constraint gap in Section~\ref{sec:limitations} left open.

\small
\bibliographystyle{jcgt}
\bibliography{krb}

\section*{Index of Supplemental Materials}

Paths are relative to the study tree \texttt{studies/kinematic\_restitution\_balancing/}.

\begin{itemize}
\item \texttt{KRB\_Whitepaper.md} --- source of truth for claims and numbers.
\item \texttt{jcgt/krb.tex}, \texttt{jcgt/krb.bib}, \texttt{jcgt/build-notes.md} --- paper source, bibliography, TeX build notes.
\item \texttt{figures/fig-energy-ablation.pdf} (and \texttt{.png}) --- Figure~\ref{fig:ablation} vector/raster.
\item \texttt{figures/fig-energy-engines.pdf} (and \texttt{.png}) --- Figure~\ref{fig:engines} vector/raster.
\item \texttt{plot-energy-ablation.py}, \texttt{plot-energy-engines.py} --- figure generators from committed CSVs.
\item \texttt{data/README.md} --- recapture notes and column layouts.
\item \texttt{data/floor-bounce-krb.csv}, \texttt{data/floor-bounce-nokrb.csv}, \texttt{data/bounce-circle-krb.csv}, \texttt{data/bounce-circle-nokrb.csv}, \texttt{data/cradle-krb.csv}, \texttt{data/cradle-nokrb.csv} --- Dataset~A ablation traces.
\item \texttt{data/dataset-b-floor-bounce-600s.csv}, \texttt{data/dataset-b-newtons-cradle-600s.csv} --- Dataset~B whole-engine traces.
\item \texttt{data/dataset-c/cradle-krb.csv}, \texttt{data/dataset-c/bounce-circle-krb.csv} --- Dataset~C $8\,\mathrm{h}$ traces (no figure).
\item \texttt{log-energy.cpp} --- headless logger for Datasets~A and~C.
\end{itemize}

\section*{Author Contact Information}

\hspace{-2mm}\begin{tabular}{p{0.5\textwidth}p{0.5\textwidth}}
Joshua Sideris \newline
Gearbox2D \newline
\href{https://gearbox2d.com}{https://gearbox2d.com} \newline
\href{mailto:josh.sideris@gmail.com}{josh.sideris@gmail.com}
&
\end{tabular}

\section*{Code and Data License}
Supplemental source code and data referenced in later revisions of this article are distributed under the ISC License (Open Source, BSD-class); see \texttt{LICENSE} in the Gearbox2D repository (\href{https://github.com/JSideris/Gearbox2D}{https://github.com/JSideris/Gearbox2D}).

\afterdoc

\end{document}
